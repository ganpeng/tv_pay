import _ from 'lodash';
import service from '../../service';

const state = {
};

const getters = {};

const mutations = {}

const actions = {}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}